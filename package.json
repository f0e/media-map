{
  "name": "show-graph-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "bun run --cwd packages/client dev",
    "server": "bun run --cwd packages/server dev"
  },
  "dependencies": {
    "@tanstack/react-router": "^1.112.0"
  }
}